Full-scene lighting equivalence milestone (7f2364c): opt-in sceneEquivalence=1 mirrors actual application draws into a separate color/depth target, omitting only colorless masks and disabling lighting stencil on that diagnostic target. The original canvas retains the complete alpha/stencil path. Static and moving-camera runs match all 839680 scene RGB pixels at Presents 1/30/60; full framebuffer hashes also match. Moving-camera run f914691f-b76e-40ad-9161-fa351c007189 has three visually inspected captures. Negative-control run 47efae03-db01-403c-a91e-4076fcb8c0cc deliberately omits diagnostic lighting and detects 268620 different scene pixels. Counts verify 15 mask and 15 stencil-lighting draws per frame. This establishes sampled optimization equivalence, not independent native conformance: shared shader/rasterization errors could affect both paths. Diagnostic allocations/readbacks stop after Present60 and are explicitly marked in telemetry; no performance claim derives from these runs. A final requirements audit remains pending.

Resource-upload batching milestone (7100c43): buffer and texture updates up to 1MiB join draws in copied ordered staging; creation, destruction, other host calls and larger uploads flush first. The unchanged 64-command/1MiB limit imposes backpressure, and a deferred rejection wakes/fails the CPU before its next synchronous operation. Original 1280x720 camera run ed3dedb4-1560-466b-9109-7595d946d38a has three inspected scene captures. Separate screenshot-free 60s trial 79aaadc8-3c92-44fc-a9bf-1b14c13dee25 produced 4529 Presents, no recorded GPU errors, 75.68 submissions/s and p50/p95/p99 12.90/15.57/20.37 ms. Last telemetry includes 31611 uploads (10892233 bytes) and 180030 draws in 54046 mixed batches; the legacy counter name drawBatches now counts mixed batches. No speedup is established by this single trial. Full-scene alpha/stencil reference validation and the final requirements audit remain pending.

Three-minute stability milestone (d86cb32): screenshot-free 1280x720 run 0e9ee678-ef0b-4525-a69e-64589e7a7a4e completed 180 seconds after first Present, with 13503 Presents and no recorded GPU errors, device loss or visibility changes. Submission rate 75.11/s; p50/p95/p99 13.02/15.61/20.84 ms (last 8192 intervals). Sampled WASM bytes 288096256, geometry bytes 60784, texture bytes 4025064, pipelines 5 and shaders 6 remained unchanged, including the final telemetry snapshot. These are scoped resource counters, not proof of leak freedom or total browser memory. Extended benchmark duration is bounded to 60–600 seconds via benchmarkSeconds; the explicit user-started four-hour control is unchanged. Original full-scene alpha/stencil reference validation and resource-update batching remain pending.

1280x720 milestone (107fc3d): original machine code enumerates virtual HKLM\SOFTWARE\Humus and consumes saved WindowedRight=1280 / WindowedBottom=720. Added generic ANSI registry enumeration and launcher DWORD preload, preserving name casing, bounds, access checks and stdcall semantics. EXE and assets remain byte-identical. Three inspected scene captures and controlled camera movement pass (bff3c8ce-a206-454b-ac39-f031b527fa8f). Separate visible, screenshot-free 60-second trial c52b9918-03cd-42d4-8de9-a1086b43978b: 4534 Presents, 75.87 submissions/s, p50/p95/p99 12.98/15.11/20.28 ms, first Present 1588.92 ms, no recorded GPU/draw errors. WASM memory remains 288096256 bytes; each color/depth logical attachment is 3686400 bytes (depth is a minimum estimate). This does not prove display FPS or completed alpha/stencil correctness. All 16 winapi tests pass; 38 patched sources reproduce on a clean pinned checkout. Launch with /humus-runtime?benchmark=1&resolution=1280x720; add captureFrames=1&cameraTest=1 only for separate evidence runs.

Draw-batching milestone (fbf8fc4): copied immutable draw packets use a 64-command / 1 MiB bounded queue, flushed before other host RPCs so updates and destruction preserve order. Deferred errors stop the CPU before its next boundary operation. Original run 48366511-c15b-47ab-942f-7f8a781e7c73 has three visually inspected scene captures and controlled camera movement. Separate screenshot-free 60-second run 72228cf0-5522-4066-a492-4465a95b5cdd produced 4644 Presents with no recorded errors: 77.66 submission/s, p50/p95/p99 12.73/14.21/20.11 ms, first Present 1619.13 ms. Counts at the last telemetry sample: 184810 draws in 55450 batches (about 70% fewer draw round trips); staging adds 1052672 bytes outside WASM. Resource uploads remain synchronous. Full reference correctness, target resolution and longer stability remain pending. Historical milestones below retain their original scope.

Repeated-startup milestone (375a680 timing trials; 34420f1 first-scene captures): three cold and three warm app-asset-cache trials each. Median first-Present submission is 1667.77 ms cold / 1592.37 ms warm. In a separate six-run capture series, inspected first-scene readback completion upper bounds are 1651.09 ms cold / 1628.68 ms warm. All six images show the original textured pillar room with lighting. Cold mode clears only this app's asset cache; warm mode verifies cached SHA-256 bodies. Warm trials avoid 24,665,061 bytes of asset/WASM bodies; JS, shader-runtime, browser compiler and OS caches are outside this definition. Cache is bounded to one active manifest, 256 entries and 128 MiB; corrupt entries are repaired, obsolete entries evicted. Screenshot-free steady run `3527410a-efd8-48c7-89b0-4d1dcf9a05b3` measures 72.93 Present submissions/s, p50/p95/p99 13.55/14.75/21.27 ms over 3967 post-warmup intervals, with no recorded draw/device errors. Recompute with `python3 scripts/summarize_startup_trials.py`. Reproduce startup modes using /humus-runtime?benchmark=1&startupTrial=1&assetCache=cold (or warm); append &captureFrames=1 for separate first-scene capture tests. Full scene alpha/stencil validation, graphics batching, target-resolution and longer stability work remain pending.

Optimized-build milestone (f0bbfc0): `scripts/build_wasm.sh release` builds the original translated binary with Rust optimization; `debug` preserves the prior mode. Debug artifacts and their hash manifest are retained locally under web/generated/debug-baseline-98ba41b. Exploratory run `0b7223b0-fe04-4d2a-8c81-7d8125cd6921` at 800x600 reached first Present in 1728.45 ms and measured 74.12 Present submissions/s after five seconds of warmup (p50/p95/p99 13.43/14.72/21.11 ms). Prior debug run measured 45558.67 ms and 29.48 submissions/s. One trial each is not a formal cold/warm series; release included one UI screenshot. Both use uncached loopback responses; browser compiler/OS cache state is uncontrolled. Optimized WASM is 22,612,475 bytes versus 18,317,250 debug. Linear memory remains 288,096,256 bytes; geometry GPU storage 60,784 bytes and texture storage 4,025,064 bytes exclude attachments/driver overhead. No exact total memory or display FPS claim. Original scene remains recognizable in optimized run; full acceptance, repeated startup trials, batching and longer stability remain pending. See evidence/performance-summary.json.

Controlled-input milestone (6e64e4d): run `75ca1907-369c-4b83-87ac-6ff1a26b2de9` presses the original default forward key, Arrow Up, after Present 1 and releases it after Present 30. Original camera matrix translation changes from ~1800 to ~28.48 while held, then remains bit-identical at Present 60 after release. Three inspected captures show the camera move from the room entrance toward a wall, with changing scene-region pixels excluding FPS. Input uses the same host queue as browser keyboard events. The initial W test did not move because W is not the EXE's default binding. First-frame state evidence records 15 alpha LESS255/stencil REPLACE draws and 15 stencil EQUAL lighting draws, with the existing diagnostic discard/stencil tests passing. Full scene pixel/reference agreement and repeatable performance/memory/stability measurements remain pending.

Current scene milestone (f4996f6 + capture revision 684bd62): original run `913e32fa-7f01-471e-9544-41c9464bf61b` produced three inspected GPU framebuffer captures of the textured pillar room with changing colored lights, at Presents 1, 30 and 60. The black-scene root cause was missing initial WM_WINDOWPOSCHANGED: application width/height stayed zero, making aspect ratio 0/0 and projection Y NaN. Initial placement now calls the registered original wndproc, and DefWindowProc provides default SIZE/MOVE delivery. The sampled original matrix is finite. Scene-region measurements exclude the top 64 source pixels containing FPS text. This is recognizable original-driven rendering, **not completed acceptance**: controlled camera input, alpha/stencil reference agreement, startup/steady-state measurements and stability remain outstanding. Capture mode adds three readbacks and must be excluded from performance runs. See evidence/scene-review.json and evidence/scene-frames/.

Current checkpoint (fe8c757): original run `cdda1549-4d2c-4717-bc29-7a3020c6ce5d` reached **288 application Presents / submitted frames**, then the bounded startup watchdog stopped execution. A browser screenshot shows the original FPS text but a black scene region: **zero verified scene frames, rendering acceptance fails**. The previous null-shader API blocker is resolved for unlit XYZ/diffuse/TEX2 draws using world/view/projection uniforms and the documented default texture stage. Hardware GPU readback confirms transform order and texture/color/alpha behavior; all shader diagnostics pass. Ten native D3D9 tests pass and 38 patched files restore identically. Next: investigate scene coordinate/state/shader correctness, capture scene-region evidence, and verify controlled input. These Present counts are not scene FPS.

Current checkpoint (10fd430): original run `6194abc7-8882-45ea-acf5-816705dd5483` retained 33 hardware GPU draws, zero Presents and zero verified scene frames. `DrawPrimitiveUP` now validates and copies bounded single-stream user vertices, expands fans/strips into lists, and converts packed D3DCOLOR into RGBA float attributes. The original billboard reaches the precise remaining failure: both shaders null, FVF `0x142`, lighting disabled. Its four 24-byte vertices normalize to six 36-byte vertices, but are not submitted without fixed-function shader support. Transform state must feed that future GPU path. Programmable UP submission uses the existing draw packet; its GPU integration is not yet independently verified. Temporary UP buffers are bounded to 16 MiB and destroyed after submission; persistent scratch reuse remains an optimization. Ten D3D9 native tests pass; clean bootstrap reproduces 38 files. [Microsoft UP semantics](https://learn.microsoft.com/en-us/windows/win32/api/d3d9/nf-d3d9-idirect3ddevice9-drawprimitiveup) require consuming user data before return and clearing stream zero; the implementation follows those rules.

The milestones below include historical state; use this checkpoint and MEASURED-RESULTS.json for the current acceptance result.

Evidence source: original archive from https://humus.name/index.php?ID=50&page=3D and scripts/inspect_pe.py, with machine-readable headers/imports/instructions in evidence/pe-inspection.json. Main.cpp is explanatory evidence, never build input.

| Area | Executable/package evidence | Current support |
|---|---|---|
| PE/x86 | i386 PE32, entry 0x0040bdb2, base 0x00400000, 925696 bytes; no delay imports | Theseus AOT; preserved original SHA-256 in dependencies.json; LNK_INFO mapping fix |
| Startup | Static MSVC CRT and x87 code; FS-based SEH, CPUID | Startup executes; callback 0x00409a00 discovered dynamically; unprefixed BT/BTS/BTR/BTC cover register and signed memory-bit-string offsets; locked forms, Float80 and other unsupported instructions trap with addresses |
| Win32 | 6 imported DLLs; kernel32/user32/advapi32/shell32/gdi32/d3d9 | Reused subset with known incomplete upstream stubs; no claim of full compatibility; explicit new missing-import traps |
| Heap | Original model preparation repeatedly calls HeapReAlloc | Implemented copy/grow/shrink, zero-new bytes and allocation-failure preservation; regression test passes |
| Virtual display | EnumDisplaySettingsA and CreateWindowExA | Single 1280×720 virtual display; EXE requests an 800×600 window; OffscreenCanvas receives those actual dimensions |
| Callbacks | RegisterClassA receives original wndproc 0x00409a00 | Callback included in translation inputs; synchronous ANSI NCCREATE/CREATE callbacks execute original code and initialize its HWND; synchronous DESTROY/NCDESTROY callbacks and thread WM_QUIT preservation implemented |
| Registry | RegOpenKeyExA, enumeration, query and writes | Empty isolated registry returns missing-key failure; writes/enumeration not implemented, trap if reached |
| Timing | QueryPerformanceFrequency/Counter | Monotonic host milliseconds, reported frequency 1000; virtual 1 MHz RDTSC counter. No physical CPU-speed or high-resolution timing claim |
| Exceptions | SetUnhandledExceptionFilter, RaiseException, RtlUnwind | Filter registration stores/replaces pointer; complete exception dispatch not implemented |
| D3D9 | Only Direct3DCreate9 statically imported | SDKVersion=31 creates a real COM object with 17 ABI slots, identity/refcounts and virtual display queries. GetDeviceCaps validates inputs and obtains the implemented programmable profile from the GPU worker (native host still returns NOTAVAILABLE); original CreateDevice allocates validated 800×600 BGRA8 and depth24plus-stencil8 attachments via a dedicated GPU worker. Device/surface COM lifetimes, descriptions, clear and presentation implemented; other device calls trap precisely |
| D3DX | Embedded D3DX9 compiler/assembler version strings 4.09.00.1221; no imported D3DX DLL | Evidence suggests statically linked compiler. Actual compiler execution and dynamic shader-validator lookup still unverified |
| Shaders | .shd HLSL files loaded by name; VS 1.1/PS 2.0 required by package and source | Actual Humus bytecode not captured yet. Reusable WASM MojoShader→SPIR-V→Naga→WGSL path handles diagnostic VS1.1/PS2.0 mov and texld; VS1.1 normalization uses actual register names and preserves explicit input semantics; uniform reflection and sparse/array register packing implemented; GPU c7 diagnostic passes. Device float/int/bool constant setters/getters preserve register bounds and float/int bits. VS1.1/PS2.0 shader COM creation, binding, GetFunction and lifetimes implemented; bounded worker bytecode storage validates with MojoShader and defers pair translation until draw. Application draw integration remains incomplete; no full-profile claim or hardcoded replacement shaders |
| Assets | ../Models/PillarRoom/Map.hmdl; DDS bases/font, PNG normal-height maps/particle, .font | Original directory relationships preserved; browser mounts each integrity-checked file with /DynamicBranching cwd |
| Geometry | Model upload and indexed draws; DrawPrimitiveUP triangle fans for lights | Vertex/index buffer COM creation, descriptors, lock/unlock and GPU uploads implemented for default/managed pools and ordinary/dynamic writes; bounded to 128 MiB GPU storage. Nested locks and other pools/flags rejected. Stream/index setters/getters now retain bound storage, preserve offsets/strides, and release it on unbind/device destruction; instancing frequencies beyond 1 are rejected. FLOAT1..FLOAT4 vertex declaration COM creation/binding/retrieval now validates and owns the element array; packed types, FVF/fixed-function, semantic-to-location mapping supports FLOAT1..FLOAT4; validated shader wrappers supply missing (0,0,1) components without changing the shader body. Packed types and application draws remain incomplete |
| Correctness | Ambient/depth first; alpha <255 passes replace stencil with 1; lighting tests stencil==1; additive blending | IR alpha-discard transform and D3D9 state mapping implemented. Diagnostic GPU tests prove LESS255 discard preserves stencil/depth and equality gates additive light. Application integration/reference comparison still pending |
| Input | PeekMessage/DispatchMessage, keyboard, cursor positioning; settings F1 | Bounded keyboard/pointer delivery through GPU worker, polling and waiting implemented; controlled camera acceptance pending |
| WebGPU | Actual adapter/device request with error/loss events | Apple/metal-3 non-fallback adapter observed; original device attachments created; no Humus draw submission |
| Baseline | No local CheerpX artifacts or verified rendering found | Unavailable; historical Linux initialization is not a performance baseline |

Before scene acceptance: validate the implemented D3D9 caps/device, shader translation, resource, alpha/stencil, input and presentation path against actual application calls; extend unsupported operations as they are encountered. Browser canvas stays blank when unsupported; no CPU framebuffer is passed off as accelerated rendering.

Texture backend milestone (8b1f424): bounded GPU-resident A8R8G8B8/X8R8G8B8 and DXT1/3/5 2D storage, explicit BC device feature negotiation, full mip uploads with guest row pitch, opaque XRGB conversion, pointer/lifetime checks, and allocation accounting. Hardware GPU readback verifies payloads, including compressed 2x2 and 1x1 tail mips; worker RPC tests pass. Base compressed dimensions must be block-aligned; partial rectangles, texture COM methods, samplers and draw binding remain unimplemented. These diagnostics are not Humus rendering acceptance. Launch controls now wait for the asset manifest before enabling Start.

Texture COM milestone (02c2f6e): IDirect3DTexture9 now has the 22-slot ABI, resource/base-texture QueryInterface identity, external and device-binding reference lifetimes, CreateTexture, GetType, GetLevelCount, GetLevelDesc, full-mip LockRect/UnlockRect, and device Set/GetTexture for pixel sampler slots 0–15. Managed and default pools with a bounded dynamic usage subset are supported; shared handles, subrect locks, dirty-tracking flags, surface-level views, autogeneration and LOD methods remain explicit failures. Writable unlocks convert guest pointers to WASM linear addresses and upload to the persistent GPU texture; read-only unlocks avoid uploads. Native tests verify ABI, pitch, lock rules and binding lifetime; they do not establish original-application texture use. Clean bootstrap reproduced all 32 patched files.

Mip surface milestone (5ed93ba): GetSurfaceLevel returns a cached IDirect3DSurface9 view of the requested texture mip. GetContainer queries the actual parent texture and retains it; surface references keep textures alive without an ownership cycle. Surface and texture LockRect/UnlockRect share staging and lock state. Rectangle locks retain full-mip row pitch and return the correct byte/block offset; compressed rectangles require aligned interior edges and permit small edge mips. Writable unlock currently uploads the full mip, preserving untouched staging bytes. Native tests cover identity, stdcall, parent lifetime, shared locks and rectangle boundaries; original application calls remain gated by unavailable shader capabilities.

Sampler milestone (0712be6): device Set/GetSamplerState cache DWORD state for 16 pixel sampler slots with D3D9 defaults. The WebGPU mapper supports wrap/mirror/clamp, point/linear magnification and minification, disabled/point/linear mip filtering, and largest-mip restrictions. Sampler descriptors are cached with a 512-entry LRU bound. Eight GPU compute sampling checks verify actual colors and mip interpolation, separately from Humus scene acceptance. Border and mirror-once addressing, anisotropic filtering, nonzero LOD bias, sRGB sampling, and displacement/multielement sampling remain unsupported; application draw submission still needs to consume the cached sampler state.

Pipeline milestone (42d9423): a reusable WebGPU pipeline builder combines stored VS/PS bytecode, semantic vertex layouts, compact float input expansion, and alpha/depth/stencil/blend/cull state. Shader-pair and pipeline caches each retain at most 128 entries with a 16 MiB accounting bound; destroyed shader handles fail before cached lookup. Dynamic stencil references do not force compilation. GPU diagnostics now use these pipelines for indexed geometry, persistent BGRA texture sampling with cached samplers, alpha-tested stencil writes, additive stencil-tested lighting, and the discard/depth probe. Five pipeline variants used two linked shader translations. This is component integration; the original executable still requires draw packet serialization and submission through its D3D9 methods.

Draw bridge milestone (c119870): DrawPrimitive and DrawIndexedPrimitive now serialize shader handles, declarations, stream/index bindings, render state and shader register bits into bounded DWORD packets. The GPU worker decodes these packets, reuses linked shader/pipeline caches, packs reflected uniforms into persistent GPU buffers, and submits actual indexed or nonindexed draws. CPU-side index checks and GPU-worker stream bounds reject invalid accesses. Point/line/triangle lists are supported; textured draw binding, strips/fans, UP calls, viewport changes and broader state remain incomplete. GPU readback and real worker RPC diagnostics pass, separately from original-executable acceptance. The native host returns NOTAVAILABLE after a valid draw packet; it never fabricates GPU success.

Textured draw milestone (3a62518): draw packets now carry all 16 pixel texture handles and sampler states. SPIR-V combined-sampler splitting exports the exact texture/sampler binding pairs and image dimensions; draw submission binds the reflected 2D resources and cached samplers alongside shader uniforms. GPU tests verify textured indexed draws at both sampler slot 0 and sparse slot 7, matching RGBA [17,121,233,255], and worker RPC submission. Missing/released/locked textures and unsupported image dimensions fail explicitly. Vertex textures, cube/volume textures, viewport changes, strips/fans and UP draw calls remain incomplete.

Viewport milestone (0c6930e): Set/GetViewport preserve DWORD rectangle fields and float depth-range bits, validate pointers and render-target bounds, and reject invalid updates without changing prior state. The initial viewport matches the actual device dimensions. Draw packets carry viewport state and GPU submission applies it dynamically, without pipeline recompilation. GPU samples prove a [4,8,16,16] viewport confines geometry and a changed MinZ changes depth-test visibility. D3D9 pixel-center compensation and broader primitive/state coverage still need validation.

Capability and initialization milestone (73f6e62): the compatibility device now reports VS1.1/PS2.0 and the implemented 2D texture, depth/stencil, blend, viewport and sampler subset. The profile is not a physical GPU identity or full D3D9 conformance claim; unsupported feature flags remain clear and format queries reject volume/cube resources. Original Humus passed its shader-version gate and reached GetSystemInfo, InterlockedCompareExchange and InitializeCriticalSection. Those runtime queries/operations are now implemented: one virtual x86 family-5 worker, 4 KiB runtime allocation granularity, sequentially consistent DWORD atomics, and reentrant single-thread critical sections. Cross-thread contention explicitly fails. Original intermediate evidence is preserved in evidence/caps-first-original-progress.json.

CPU comparison milestone (75acee2): the original binary passed FUCOMPP at 0x0043a489 after implementing FCOM/FUCOM comparison status and one/two-pop forms, then exposed JNP at 0x0043a490. JP/JNP now consume the existing low-byte parity flag. Runtime tests cover ordered/unordered values, signed zero, NaN exception distinction, double pop and 512 parity inputs. This preserves the inherited f64 approximation; full x87 80-bit precision, rounding modes and unmasked exception delivery remain incomplete. The actual pre-fix trace also shows a 512×512 DXT3 texture created and all ten mip levels uploaded through the D3D9 COM path. No scene frame is implied.

Absolute-value milestone (83b34bd): parity branch execution advanced to FABS at 0x0043a4ba. The translator now emits the runtime f64 absolute-value operation for ST0. Rebuilt original machine-code translation and WASM; clean restore reproduces all 36 patched files.

Callback discovery milestone (5f8b7ec): observed missing targets 0x004233f4 and 0x0043f5c8 are PUSH immediates in executable code. Enabling the existing immediate-pointer scanner discovers callback targets from the unmodified binary, raising decoded code coverage to 95.2% (56,431 blocks). This is static coverage, not executed coverage. Unsupported SSE/AVX register operands now emit an exact runtime failure instead of crashing translation through MMX operand decoding.

Latest original execution (0b3f323, run 35e53c33-2546-4efd-9f76-2667908383ef): both previously missed callbacks execute after immediate scanning. The next exact blocker is FLDLN2 at 0x004221b0. Zero application Presents and zero scene frames; no correctness or FPS acceptance. The final WASM build also diagnoses unsupported 16-bit addressing overrides at runtime rather than generating invalid Rust.

Logarithm milestone (62c1d5b): x87 constant loads (LN2, LOG2E, LOG2(10), LOG10(2), PI) and FYL2X positive-finite computation now translate. FYL2X writes ST1 and pops once; unsupported exceptional inputs fail explicitly. Eleven native runtime tests pass and the WASM build is verified. Original run d8a5bb8e-b8d5-4401-83f4-967fcd91b429 passes FLDLN2/FYL2X and stops at FNSAVE [ecx+8] at 0x004b9da2, followed by a FRSTOR path at 0x004b9dae. Required next work is the actual protected-mode x87 state memory image and restoration, not a no-op. Intel instruction reference: https://cdrdv2-public.intel.com/868140/253666-089-sdm-vol-2a.pdf . No original Present or scene frame yet.

State and math milestone (64e18a6/a2d8aa7/fd8a4ca): FNSAVE/FRSTOR implement the 108-byte protected-mode image, including 80-bit encoding of represented f64 values, control/status/TOP/tags and instruction/operand metadata. Reset marks the stack empty while preserving register payloads. Restore rejects extended precision beyond f64, unsupported stack shapes and unmasked exceptions. FRNDINT honors all four RC modes; F2XM1 and FSCALE implement bounded finite input paths. PUSHFD/POPFD support virtual user-mode flag save/restore and CPUID ID-bit toggling, preserving IF/IOPL restrictions and rejecting single-step. Fifteen native runtime tests, WASM build and clean restore of 37 patched files pass. Intel layout source: https://cdrdv2-public.intel.com/782149/253665-sdm-vol-1.pdf (figure 8-9).

Latest original run aa232064-f425-4180-9892-a0fc71a74bec on fd8a4ca executes past these CPU blockers and stops at advapi32!RegCreateKeyExA, prior_builtin_return=0x0040272f, esp=0x00013ebc. There are still zero Present calls and zero verified scene frames. Registry API work and inspection of the caller are next; the run does not establish successful shader creation or scene rendering.

Registry and luminance milestone (dd66795/cc749ef/8d37e91): bounded per-instance virtual ANSI registry keys, handles and typed values now implement create/open/set/query/close, access rights and short-buffer semantics. ASCII names and 1 MiB value-data budget; host registry untouched, no cross-launch persistence. Heap trace noise is filtered before the worker log quota so application diagnostics survive. This exposed the real loading rejection: CreateTexture 32×32, six mips, D3DFMT_L8 (50), followed by "Couldn't create texture" and ExitProcess. L8 now uses one-byte guest rows and GPU RGB luminance replication with alpha one, with four-byte GPU allocation accounting. Hardware readback verifies pitch and 1×1 mip behavior. Original execution passes that rejection and reaches FIDIV at 0x00407459. The integer-operand x87 add/subtract/multiply/divide family now translates at d39227f.

Latest original run ad15ba8d-6f70-4887-a48b-0875a111823f on d39227f: FIDIV executes; FPTAN at 0x0040bd32 is the next exact blocker. Zero original Present calls and verified scene frames.

Tangent and transform milestone (765449f/98db63e): FPTAN computes tangent and pushes one, preserves signed zero, and reports out-of-range C2 without changing the stack; FPU state save/restore preserves standalone C2. Sixteen native runtime tests pass. Set/GetTransform store validated D3D9 view/projection/world/texture matrix state with identity defaults, preserving exact DWORD values; programmable shaders still receive their explicit constants and fixed-function drawing remains unsupported. Seven native D3D9 tests and WASM build pass; clean restore reproduces 37 patched files. Original run a3197b97-f12b-4c44-8d3f-73e570b6c3d1 passes FPTAN and SetTransform, then stops at SetFVF(0x142), return address 0x00407109. No original Present or verified scene frames yet.

FVF and actual GPU draw milestone (a6a9e38/d5b6970): Set/GetFVF converts supported non-blended FVF layouts into owned declarations, reuses binding/reference handling, and clears FVF state on explicit declaration selection. Tests verify 0x142 position/color/texture layout, dimensions, invalid formats, stdcall and lifetime. Packed color remains represented in declarations but its GPU input mapping and fixed-function rendering are not implemented yet. Microsoft mapping reference: https://learn.microsoft.com/en-us/windows/win32/direct3d9/mapping-fvf-codes-to-a-directx-9-declaration .

Original GPU draws exposed Naga 30 serializing f32::MAX as a rounded decimal just above the finite WGSL range. Translator output now canonicalizes this literal to exact hexadecimal across base translation and subsequent IR rewrites. Browser shader diagnostics pass, including positive/negative maximum-float compilation. Original run 2ff5cf82-f247-4488-a331-698430d185b8 records 33 retained GPU draw submissions and no retained draw rejections, then stops at DrawPrimitiveUP(TRIANGLEFAN,2,guest pointer,24-byte stride), return 0x00407121. This is the first verified original-driven GPU draw submission milestone; zero Present calls and scene frames mean rendering correctness remains unverified.
